import { Canvas, Meta, Story } from "@storybook/addon-docs";
import Input from "./Input";

<Meta title="MDX/Atoms/Input/Input" component={Input} />

export const Template = (args) => <Input {...args} />;

Component "Input" type `text`

<Canvas>
  <Story
    name="Input text"
    args={{
      placeholder: "Type in text...",
      width: "250px",
      height: "40px",
      fontSize: "16px",
      color: "black",
      type: "text",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Input password"
    args={{
      placeholder: "Type in password...",
      width: "250px",
      height: "40px",
      fontSize: "16px",
      color: "black",
      type: "password",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Input email"
    args={{
      placeholder: "Type in email...",
      width: "250px",
      height: "40px",
      fontSize: "16px",
      color: "black",
      type: "email",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Input date"
    args={{
      placeholder: "Type in date...",
      width: "250px",
      height: "40px",
      fontSize: "16px",
      color: "black",
      type: "date",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Input range"
    args={{
      placeholder: "Type in range...",
      width: "250px",
      height: "40px",
      fontSize: "16px",
      color: "black",
      type: "range",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Input week"
    args={{
      placeholder: "Type in week...",
      width: "250px",
      height: "40px",
      fontSize: "16px",
      color: "black",
      type: "week",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

Component `Input` nhận vào các giá trị:

`type:`:`text`|`password`|`button`
| `checkbox`
| `color`
| `date`
| `datetime-local`
| `email`
| `file`
| `hidden`
| `image`
| `month`
| `number`
| `password`
| `radio`
| `range`
| `reset`
| `search`
| `submit`
| `tel`
| `text`
| `time`
| `url`
| `week`

`outline`: `string`

`border`: `string`, đường viền của Input

`fontFamily`: `string`, font chữ,

`fontSize`: `string`, kích thước chữ

`color`: `string`, màu chữ

`width`: `string`, chiều dài Input(px)

`height`: `string`, chiều rộng Input(px)

`background`: `string`, màu nền Input
