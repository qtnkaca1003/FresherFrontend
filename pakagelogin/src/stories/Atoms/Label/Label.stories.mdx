import { Canvas, Meta, Story } from "@storybook/addon-docs";
import Label from "./Label";

<Meta title="MDX/Atoms/Label/Label" component={Label} />

export const Template = (args) => <Label {...args} />;

Component `Label`

<Canvas>
  <Story
    name="Label"
    args={{
      fontFamily: "Poppins-Regular",
      fontWeight: "bold",
      fontSize: "14px",
      color: "red",
      lineHeight: "1.5",
      text: "Label",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

Component `Label` nhận vào các giá trị:

`text`: `string`,

`className`: `string`

`fontWeight`:`500`|`600`| `700`|`800`|`900`|`bold`|`bolder`|`lighter`|`normal`|`initial`

`padding`: `string`

`fontFamily`: `string`, font chữ

`fontSize`: `string`, kích thước chữ

`color`: `string`, màu chữ