import { Canvas, Meta, Story } from "@storybook/addon-docs";
import Title from "./Title";

<Meta title="MDX/Atoms/Title/Title" component={Title} />

export const Template = (args) => <Title {...args} />;

Component `Title`

<Canvas>
  <Story
    name="Title"
    args={{
      fontFamily: "Poppins-Regular",
      fontWeight: "bold",
      fontSize: "39px",
      color: "#333",
      lineHeight: "1.5",
      text: "Title ",
      textAlign: "center",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

Component `Title` nhận vào các giá trị:

`textAlign`: `left` | `center` | `right`

`text`: `string`,

`fontWeight`:`500`|`600`| `700`|`800`|`900`|`bold`|`bolder`|`lighter`|`normal`|`initial`

`padding`: `string`

`fontFamily`: `string`, font chữ

`fontSize`: `string`, kích thước chữ

`color`: `string`, màu chữ
