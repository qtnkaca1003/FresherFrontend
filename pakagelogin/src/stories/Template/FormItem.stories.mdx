import { Canvas, Meta, Story } from "@storybook/addon-docs";
import Login from "./Index";


<Meta title="MDX/Template/Login/Login" component={Login} />

export const Template = (args) => <Login {...args} />;

Component `Login`:

<Canvas>
  <Story
    name="FormLogin"
    args={{
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

Component `Login` kết hợp từ FormItem và OrSignUp